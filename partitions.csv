# 4 MB flash. `needle` holds verbatim copies of the archive's hottest and
# smallest tensors (tools/prepare_sd.py): the embedding (tied output head,
# read every decoding step), the tokenizer, every FP16/FP32 vector and the
# mHC maps. The 2-bit matrices stay on the SD card.
# Name,   Type, SubType, Offset,   Size
nvs,      data, nvs,     0x9000,   0x6000
factory,  app,  factory, 0x10000,  0x80000
needle,   data, 0x40,    0x90000,  0x370000
